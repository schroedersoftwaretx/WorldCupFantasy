node_modules/
dist/
.next/
.env
.env.local
*.log
.DS_Store
coverage/
*.tsbuildinfo

# Local Chromium profile for the SofaScore browser-fetch (Cloudflare clearance,
# cache, cookies). Machine-specific, large (~1700 files) — never commit.
.sofa-profile/

# Vitest writes a transpiled config beside vitest.config.ts on each run.
vitest.config.ts.timestamp-*.mjs
